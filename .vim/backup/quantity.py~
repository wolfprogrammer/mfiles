"""Unit  Factors

This module implements factos of SI units and physical constants
the system works with standard units, example:
when we are going to make the calculations using a pressure quantity
we write  x= 3 # Mpa , in this module we can do   x=3*Mpa and then
we get x= 3*1e6.


"""


class quantity():
	val=0
	unit=0

	def __add__(self,other):
		qn=self.__class__() # Build a new transfer function
		if isinstance(other,int) or isinstance(other,float):
			qn.val=self.val+other
		if isinstance(other,quantity):
			qn.val=self.val+other.val
		qn.unit=self.unit
		return qn


		#def __mul__(self,other):

 		
	def __str__(self):
		return "%s %s " %(str(self.val),self.unit)
 
def qnt(val=0,unit=""):
	q=quantity()
	q.val=val
	q.unit=unit
	return q



# Physical constants
g0=9.80665 # m/s2 

# Lenght factors
um=1.0e-6
mm=1.0e-3
cm=1.0e-2
km=1.0e3
IN=0.0254  # 1 in =0.0254 meters
ft=0.3048  # 1ft =0.3048 meters


#  Pressure factors  in terms of Pa
KPa=1.0e3
Mpa=1.0e6       
GPa=1.0e9
psi= 6.895*1e3
kpsi= psi*1e3
#mmHg=
bar=1e5  
torr=133.322
atm=1.01325*1e5


# Force factors
kN=1e3
MN=1e6
GN=1e9
Lb=4.44822162
libras=Lb
pounfs=Lb
kgf=g0

# Volume


#  Temperature
