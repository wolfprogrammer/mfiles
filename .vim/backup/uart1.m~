#!/usr/bin/octave
pkg load instrument-control
 
s1 = serial("/dev/ttyUSB0", 19200) 

# Flush input and output buffers
srl_flush(s1) 

while(true)

	data=char(srl_read(s1,48))
        fprintf('%c',char(data))
	ch=kbhit();
	srl1_write(s1,ch)

	if(ch=='q') 
		break
	endif

endwhile

